# Minimal requirements for EvoSim project
# Pins chosen to be recent and broadly compatible with Python 3.10+ (adjust if using a different interpreter)
numpy>=1.24,<2.0
pandas>=1.5,<2.0
matplotlib>=3.6,<4.0
scipy>=1.9,<2.0
PyYAML>=6.0,<7.0
